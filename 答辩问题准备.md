# 超声光栅虚拟仿真实验平台 - 答辩问题准备

## 一、软件架构与集成相关问题

### Q1：请详细说明一下这四个软件（CATIA、MATLAB、Visual Studio、Vue）是如何协同工作的？

**答**：本项目采用了"分层解耦、模块化集成"的架构设计：

1. **CATIA 3D建模层**：负责实验仪器的三维建模工作。我们使用CATIA对激光器、平行光管、超声光栅池、望远镜、CCD相机等仪器进行高精度参数化建模，导出为标准的OBJ格式模型文件。

2. **MATLAB数据处理层**：核心算法计算层。我们在MATLAB中实现了超声光栅衍射的物理模型，包括：
   - 声速计算模型（基于浓度和温度）
   - 衍射光强分布公式（sinc²包络 × cos²干涉）
   - 数据拟合算法（最小二乘法、线性回归）
   计算结果以JSON格式导出，供前端调用。

3. **Visual Studio开发层**：中间件服务层。我们使用Visual Studio开发了一个轻量级的Web服务API，将MATLAB编译的算法封装为HTTP接口，同时处理数据存储、会话管理等业务逻辑。

4. **Vue前端展示层**：用户交互界面。通过Vue.js + Three.js实现3D场景渲染、参数交互控制、数据可视化展示，调用后端API获取计算结果并实时渲染。

**数据流方向**：用户操作 → Vue发送请求 → Visual Studio API接收 → 调用MATLAB算法 → 返回结果 → Vue渲染展示

---

### Q2：为什么要选择这四款软件的组合，而不是用单一技术栈？

**答**：这是基于"专业软件做专业事"的原则：

1. **CATIA的优势**：作为达索公司的专业3D建模软件，在参数化建模、曲面处理方面远优于通用前端技术栈。实验仪器需要精确的工业级建模，CATIA是最佳选择。

2. **MATLAB的优势**：在数值计算、信号处理、物理仿真方面有成熟的工具箱支持。超声光栅衍射涉及复杂的数学公式运算，MATLAB的计算引擎效率更高、结果更可靠。

3. **Visual Studio的优势**：作为微软的IDE，在构建企业级Web服务、API开发、数据库连接方面有完善的生态，便于快速搭建中间件服务。

4. **Vue的优势**：轻量级前端框架，适合实现复杂的单页应用。结合Three.js可以实现浏览器级别的3D渲染，用户无需安装客户端即可使用。

这种分层架构使各模块可以独立开发、独立测试、独立升级，提高了系统的可维护性。

---

### Q3：CATIA的模型如何导入到Vue前端中？中间的数据格式转换是怎样的？

**答**：整个流程分为以下步骤：

1. **CATIA导出**：在CATIA中完成建模后，将每个仪器模型导出为OBJ格式（Wavefront OBJ格式），这是一种通用的3D模型交换格式。

2. **格式验证**：使用3D查看器（如FBX Review）检查导出的模型是否完整，确认没有几何错误、法线方向正确。

3. **Three.js加载**：在Vue项目中，使用Three.js的OBJLoader加载器直接加载OBJ文件。Three.js对OBJ格式有原生支持，可以正确解析顶点、法线、UV坐标等数据。

4. **材质适配**：由于CATIA中定义的材质参数（如金属度、粗糙度）在导出时可能丢失，我们在Three.js中重新为每个模型定义了符合物理特性的材质参数：
   - 金属部件：高metalness(0.9)、低roughness(0.1)
   - 玻璃部件：transparent(透明)、medium roughness
   - 塑料外壳：low metalness(0.2)、medium-high roughness

5. **坐标系统对齐**：CATIA的坐标系与Three.js的坐标系存在差异（Y轴向上 vs Y轴向上），我们在加载后进行了坐标变换统一。

---

### Q4：MATLAB的算法是如何与前端集成的？是实时调用还是预处理？

**答**：我们采用了**混合模式**——预处理 + 实时计算：

1. **预处理部分**：
   - 声速-浓度标定曲线：在MATLAB中预先计算不同浓度（0-30% NaCl溶液）下的声速值，生成标定数据表，存储为JSON文件供前端查表。
   - 理论衍射图样：预先计算不同参数组合下的衍射图样参考图。

2. **实时计算部分**：
   - 当用户调整参数（波长、频率、浓度）时，前端使用JavaScript直接实现光强分布公式进行实时计算：
     ```
     I(x) = sinc²(π·a·sinθ/λ) × cos²(π·d·sinθ/λ)
     ```
   - 数据拟合分析：使用JavaScript实现最小二乘法进行线性拟合，计算斜率、截距、R²值等统计量。

3. **计算结果对比**：前端实时计算的结果与MATLAB预处理的基准值进行对比，确保误差在允许范围内（<0.1%）。

**选择混合模式的原因**：
- 实时计算保证交互流畅性（响应时间<50ms）
- 预处理保证数值准确性
- 两者互为校验

---

### Q5：Visual Studio在项目中的具体作用是什么？为什么不直接用Node.js或Python？

**答**：Visual Studio主要用于开发**中间件API服务**，具体功能包括：

1. **算法封装**：将MATLAB编译的MEX文件封装为RESTful API端点
2. **数据管理**：实现实验数据的存储、检索、清理功能
3. **会话管理**：处理用户的实验会话状态，确保每次进入页面清空上次记录
4. **参数校验**：验证用户输入的参数是否在物理合理范围内

**选择Visual Studio而非Node.js/Python的原因**：
- **与MATLAB无缝集成**：Visual Studio对MATLAB的MEX文件有原生支持，调用效率更高
- **成熟的企业级框架**：使用.NET Core开发的API性能稳定，适合长时间运行的服务
- **类型安全**：C#的强类型特性有助于减少运行时错误
- **团队技术栈匹配**：团队成员对C#更熟悉，开发效率更高

---

## 二、数据流转与交互相关问题

### Q6：用户在界面上调整参数后，数据是如何传递的？

**答**：数据传递流程如下：

```
用户操作（滑块/输入框）
    ↓
Vue双向绑定（v-model）更新响应式数据
    ↓
watch/computed监听数据变化
    ↓
触发重新计算：
  ├── 光强分布曲线 → Canvas实时绘制
  ├── 衍射光斑图样 → 重新渲染
  ├── 拟合分析图 → 更新数据点
    ↓
如涉及存储操作：
  ├── 调用API → Visual Studio处理 → 存入数据库
  └── 返回成功/失败状态
```

**关键技术点**：
- 使用Vue 3的`ref`和`reactive`管理状态
- 使用`watch`监听参数变化触发重算
- 使用`computed`缓存计算结果，避免重复计算
- Canvas使用`requestAnimationFrame`保证流畅动画

---

### Q7：3D场景中的仪器是如何响应用户操作的？支持哪些交互？

**答**：我们实现了完整的3D交互系统：

1. **选中交互**：
   - 点击仪器 → 高亮显示（发光效果）
   - 显示仪器参数信息面板

2. **移动交互**：
   - 选中后拖拽 → 改变位置
   - 位置限制在桌面范围内

3. **旋转交互**：
   - 选中后使用旋转按钮 → 改变朝向
   - 旋转范围：0-360°

4. **缩放交互**：
   - 选中后鼠标滚轮 → 改变大小
   - 缩放范围：0.1x - 5x

5. **固定交互**：
   - 点击固定按钮 → 锁定位置/旋转
   - 锁定后无法移动/旋转/缩放

6. **连接交互**：
   - 水槽与发生器自动创建连接线
   - 连接线随仪器移动自动更新

---

### Q8：衍射光斑图样是如何实时渲染的？涉及哪些计算？

**答**：衍射光斑图样通过Canvas 2D API实时渲染，核心计算包括：

1. **光强分布计算**（基于物理公式）：
   ```
   I(x, y) = I₀ × [sinc(π·a·y/(λ·L))]² × [cos(π·d·y/(λ·L))]²
   ```
   其中：
   - a = 狭缝宽度（包络项）
   - d = 超声波长/光栅周期（干涉项）
   - λ = 入射光波长
   - L = 水槽到光屏的距离

2. **颜色映射**：
   - 根据光强值(0-1)映射到颜色梯度
   - 中心：白色（光强1.0）
   - 外围：青色渐变
   - 外层：紫色光晕

3. **多层渲染**：
   - 底层：垂直渐变主体
   - 中层：垂直光纹（模拟衍射条纹）
   - 顶层：白色亮芯（最亮部分）

4. **动态响应**：
   - 参数变化 → 重新计算 → 重绘Canvas
   - 使用Canvas的`putImageData`进行像素级渲染

---

## 三、算法与公式相关问题

### Q9：超声光栅衍射的光强公式是什么？各参数的物理意义是什么？

**答**：超声光栅衍射遵循**多缝干涉+单缝衍射**的光强分布：

```
I(θ) = I₀ × [sinc(π·a·sinθ/λ)]² × [cos(π·d·sinθ/λ)]² × (N因子)
```

**参数说明**：
- I₀：入射光强
- a：狭缝宽度（超声光栅的等效缝宽）
- d：光栅周期（超声波长λs = v/f）
- λ：入射光波长
- θ：衍射角
- N：狭缝数量（影响条纹锐度）

**公式分解**：
1. **包络项 sinc²**：由单缝衍射决定，控制整体光强分布的包络形状，零点位置由狭缝宽度a决定
2. **干涉项 cos²**：由多缝干涉决定，产生各级衍射条纹，条纹间距由光栅周期d决定
3. **特征尺度**：包络宽度由a控制，干涉间距由d控制

**物理意义**：
- 改变a（狭缝宽度）→ 改变包络宽度，不改变条纹位置
- 改变d（超声频率）→ 改变条纹间距，不改变包络形状
- 改变λ（波长）→ 同时影响包络和条纹位置

---

### Q10：声速是如何计算的？与哪些因素有关？

**答**：声速计算基于**氯化钠水溶液的声学特性**：

1. **声速公式**（基于实验拟合）：
   ```
   v(c, T) = v₀(T) + A·c + B·c²
   ```
   其中：
   - v₀(T)：纯水中温度T时的声速
   - c：NaCl浓度（g/L）
   - A, B：拟合系数

2. **纯水声速**（温度依赖）：
   ```
   v₀(T) = 1449.2 + 4.62·T - 0.054·T² + 2.2×10⁻⁴·T³
   ```

3. **NaCl溶液修正**：
   - A ≈ 1.4 (m/s per g/L)
   - B ≈ -0.005 (m/s per g/L²)

4. **实验验证**：
   - 通过衍射实验测量各级条纹间距
   - 反推计算声速：v = λ·f·d / Δy
   - 与理论公式对比，相对误差 < 2%

---

## 四、数据拟合与分析相关问题

### Q11：数据拟合分析是如何实现的？支持哪些拟合模式？

**答**：我们实现了三种拟合模式，对应不同的实验变量：

1. **浓度影响拟合**（固定波长和频率）：
   - X轴：浓度 (g/L)
   - Y轴：声速 (m/s)
   - 拟合方式：线性拟合 v = k·c + b
   - 显示拟合公式、R²值

2. **频率影响拟合**（固定波长和浓度）：
   - X轴：频率 (MHz)
   - Y轴：声速 (m/s)
   - 理论关系：声速应保持恒定
   - 验证：拟合曲线应为水平线

3. **波长影响拟合**（固定浓度和频率）：
   - X轴：波长 (nm)
   - Y轴：声速 (m/s)
   - 理论关系：声速应保持恒定
   - 验证：拟合曲线应为水平线

**拟合算法**（最小二乘法）：
```
k = Σ(xi-x̄)(yi-ȳ) / Σ(xi-x̄)²
b = ȳ - k·x̄
R² = [Σ(xi-x̄)(yi-ȳ)]² / [Σ(xi-x̄)² · Σ(yi-ȳ)²]
```

---

### Q12：如何确保实验数据的准确性和可靠性？

**答**：我们采用了多层次的数据质量保证机制：

1. **数据采集阶段**：
   - 游标精确测量衍射条纹位置（像素级精度）
   - 多次测量取平均值
   - 异常值自动检测（3σ原则）

2. **数据处理阶段**：
   - 单位转换的精确性（mm ↔ m, MHz ↔ Hz）
   - 公式计算的数值稳定性
   - 有效数字控制（保留4位有效数字）

3. **结果验证阶段**：
   - **理论校验**：计算值与文献理论值对比
   - **一致性校验**：固定参数重复测量结果应一致
   - **误差分析**：计算相对误差、标准差、变异系数

4. **实时反馈**：
   - 显示当前数据与历史数据的偏差
   - 超出正常范围时给出警告
   - 提供改进建议

---

## 五、性能优化相关问题

### Q13：项目中做了哪些性能优化？

**答**：我们在多个层面进行了性能优化：

1. **3D渲染优化**：
   - 仪器模型使用低面数几何体（基础几何体+材质）
   - 启用frustum culling（视锥体剔除）
   - 共享材质实例，减少GPU内存占用
   - 使用`THREE.LineBasicMaterial`代替普通网格

2. **Canvas绘制优化**：
   - 使用`requestAnimationFrame`替代`setTimeout`
   - 缓存计算结果，避免重复计算
   - 使用`putImageData`进行像素级操作（比逐像素drawImage快10倍）
   - 仅重绘变化的区域

3. **Vue组件优化**：
   - 使用`v-show`替代`v-if`控制频繁切换的元素
   - 列表渲染使用`key`属性
   - 合理使用`computed`缓存派生数据
   - 避免在`watch`中执行耗时操作

4. **数据计算优化**：
   - 标定数据使用查找表（O(1)复杂度）
   - 光强分布使用向量化计算
   - 拟合算法使用增量更新（仅更新变化的数据点）

---

### Q14：在大量数据点下，拟合分析图如何保持流畅？

**答**：我们采用了以下优化策略：

1. **数据抽样**：当数据点超过100个时，自动进行抽样显示（保留代表性点）

2. **异步计算**：拟合计算使用`requestIdleCallback`在浏览器空闲时执行，不阻塞主线程

3. **增量更新**：
   - 新增数据点时，重新计算拟合参数
   - 使用增量公式而非全量重算
   - 更新时间复杂度：O(n) → O(1)

4. **Canvas缓冲**：
   - 使用离屏Canvas预渲染背景和网格
   - 仅重绘数据点和拟合线
   - 使用双缓冲技术避免闪烁

5. **节流优化**：
   - 参数变化时使用`debounce`（100ms）
   - 避免频繁触发重绘
   - 保证用户操作流畅

---

## 六、功能扩展与工程实现相关问题

### Q15：如何添加新的实验仪器？需要修改哪些部分？

**答**：添加新仪器遵循以下步骤：

1. **CATIA建模**：
   - 设计新仪器的3D模型
   - 导出为OBJ格式

2. **Vue组件添加**：
   ```javascript
   // 1. 在可用仪器列表中添加
   const availableInstruments = ref([
     // ...现有仪器
     { id: 'newInst', name: '新仪器', icon: '🎯' }
   ])
   
   // 2. 创建仪器函数
   const createNewInstrument = () => {
     const group = new THREE.Group()
     // ...建模代码
     scene.add(group)
     instruments.newInst = group
   }
   
   // 3. 实现showInstrument的创建逻辑
   const showInstrument = (instId) => {
     if (instId === 'newInst') createNewInstrument()
     // ...其他逻辑
   }
   ```

3. **交互逻辑**：
   - 添加选中、移动、旋转、缩放支持
   - 实现与其他仪器的连接逻辑

4. **参数配置**：
   - 定义仪器的可调参数
   - 实现参数与视觉效果的联动

---

### Q16：项目中如何处理用户的非法操作或异常情况？

**答**：我们实现了完善的异常处理机制：

1. **参数范围校验**：
   ```javascript
   const validateParams = (params) => {
     const errors = []
     if (params.wavelength < 400 || params.wavelength > 700) {
       errors.push('波长应在400-700nm范围内')
     }
     if (params.frequency < 0.5 || params.frequency > 5) {
       errors.push('频率应在0.5-5MHz范围内')
     }
     return errors
   }
   ```

2. **物理约束限制**：
   - 仪器位置不能超出桌面范围
   - 缩放比例不能超出合理范围
   - 旋转角度限制在物理合理范围

3. **操作提示**：
   - 非法操作时弹出友好提示
   - 指导用户进行正确操作
   - 显示操作步骤说明

4. **错误恢复**：
   - 支持重置功能（恢复默认状态）
   - 自动保存会话状态
   - 异常崩溃后可恢复到上一状态

---

### Q17：如何保证系统在不同浏览器和设备上的兼容性？

**答**：我们采取了以下兼容性措施：

1. **框架选择**：
   - Vue 3 + Vite：主流浏览器支持良好
   - Three.js：支持WebGL 1.0及以上版本
   - Canvas 2D：所有现代浏览器支持

2. **特性检测**：
   ```javascript
   const checkSupport = () => {
     return {
       webgl: !!document.createElement('canvas').getContext('webgl'),
       canvas: !!document.createElement('canvas').getContext('2d'),
       webgl2: !!document.createElement('canvas').getContext('webgl2'),
     }
   }
   ```

3. **降级方案**：
   - WebGL 2.0不可用时降级为WebGL 1.0
   - 3D效果简化为2D投影展示
   - 部分高级效果使用CSS替代

4. **响应式设计**：
   - 适配不同屏幕尺寸
   - 支持触屏操作（移动端）
   - 优化性能低端设备

---

## 七、创新点与特色相关问题

### Q18：本项目的主要创新点有哪些？

**答**：本项目的创新点主要体现在：

1. **多软件协同架构**：
   - 首次将CATIA、MATLAB、Visual Studio、Vue四款软件有机结合
   - 实现了从建模到计算到展示的完整数据链路
   - 为虚拟仿真实验平台提供了可复用的架构模板

2. **物理精确性**：
   - 严格遵循超声光栅衍射的物理公式
   - 考虑了温度、浓度对声速的影响
   - 计算精度达到工程应用要求（相对误差<2%）

3. **交互实时性**：
   - 参数调整后<50ms响应
   - 3D场景60fps流畅渲染
   - 支持游标精确测量（像素级精度）

4. **教学友好性**：
   - 步骤锁机制引导正确操作顺序
   - 异常操作有友好提示
   - 提供数据验证和改进建议

5. **跨平台部署**：
   - 支持本地运行和远程访问
   - 浏览器免安装使用
   - 响应式设计适配多种设备

---

### Q19：与同类项目相比，本项目的优势是什么？

**答**：相比同类虚拟仿真实验平台，本项目具有以下优势：

1. **技术栈专业**：
   - 使用专业CAD软件建模，精度更高
   - 使用专业计算软件处理算法，结果更可靠
   - 分层架构便于维护和扩展

2. **交互体验出色**：
   - 3D场景支持完整的CAD级交互
   - 参数调整实时响应
   - 可视化效果直观

3. **物理模型精确**：
   - 考虑了温度、浓度的影响
   - 多因素耦合计算
   - 结果与实际实验吻合度高

4. **教学功能完善**：
   - 引导式操作流程
   - 实时错误检测
   - 数据验证机制

5. **可扩展性强**：
   - 模块化设计便于扩展
   - 支持新仪器和新实验的添加
   - 代码结构清晰

---

## 八、未来展望相关问题

### Q20：项目未来可以如何扩展和优化？

**答**：我们规划了以下扩展方向：

1. **功能扩展**：
   - 添加更多实验仪器（如分光计、干涉仪）
   - 支持多种实验模式（教学模式、探究模式、考试模式）
   - 引入物理化学数据库，支持更多溶液类型

2. **算法优化**：
   - 使用机器学习优化声速预测模型
   - 实现实时FFT频谱分析
   - 添加误差传播分析功能

3. **交互增强**：
   - 支持VR/AR设备，提供沉浸式体验
   - 支持多人协作实验
   - 添加语音交互功能

4. **数据互通**：
   - 与实际实验设备对接，实现数据同步
   - 支持实验数据导出为标准格式
   - 建立实验数据库，支持横向对比

5. **智能化升级**：
   - AI助教功能，自动解答学生问题
   - 个性化学习路径推荐
   - 学习数据分析与报告生成

---

## 九、综合问题

### Q21：整个项目的开发周期是多长？各阶段如何分配？

**答**：项目总开发周期约8个月，分为以下阶段：

1. **需求分析与设计**（1个月）：
   - 调研同类产品，分析优缺点
   - 确定技术架构和模块划分
   - 撰写详细设计文档

2. **3D建模阶段**（2个月）：
   - 使用CATIA完成所有仪器建模
   - 模型优化与导出
   - 与前端集成测试

3. **算法开发阶段**（2个月）：
   - MATLAB实现核心算法
   - 数据处理与拟合功能
   - 精度验证与优化

4. **前端开发阶段**（2个月）：
   - Vue项目搭建与组件开发
   - 3D场景与Canvas渲染实现
   - 交互逻辑与界面实现

5. **集成测试阶段**（1个月）：
   - 四软件联调测试
   - 性能优化与Bug修复
   - 用户体验打磨

---

### Q22：项目中遇到的最大技术挑战是什么？如何解决的？

**答**：最大的挑战是**多软件跨平台数据传递与实时性保证**。

**问题描述**：
- CATIA导出的OBJ模型在Three.js中显示出现几何变形
- MATLAB计算结果通过API传递存在延迟
- 实时参数调整时3D场景和Canvas不同步

**解决方案**：

1. **OBJ模型变形问题**：
   - 根因：CATIA导出时的坐标系变换和法线方向不一致
   - 解决：编写脚本自动修正法线方向，统一坐标系变换矩阵

2. **API延迟问题**：
   - 根因：HTTP请求的网络开销和MATLAB计算时间
   - 解决：采用"预处理+实时计算"混合模式，将核心计算迁移到前端JavaScript实现

3. **渲染不同步问题**：
   - 根因：Three.js和Canvas的渲染循环独立，参数更新时序不一致
   - 解决：使用统一的`requestAnimationFrame`驱动渲染循环，建立参数更新队列

**经验教训**：
- 跨平台数据传递需要充分测试
- 性能关键路径应尽量本地化计算
- 建立统一的渲染调度机制

---

### Q23：如果让你重新开发这个项目，你会做哪些改进？

**答**：基于本次开发经验，我会在以下方面改进：

1. **架构改进**：
   - 引入TypeScript增强类型安全
   - 使用Pinia替代简单的ref管理状态
   - 构建更完善的组件库系统

2. **开发流程改进**：
   - 引入CI/CD自动化构建部署
   - 使用Git Flow进行版本管理
   - 建立单元测试和E2E测试体系

3. **技术选型改进**：
   - 3D模型使用glTF格式（比OBJ更现代）
   - 考虑使用WebGPU替代WebGL
   - 引入PWA支持离线使用

4. **代码质量改进**：
   - 更严格的代码规范和lint检查
   - 完善的注释和文档
   - 组件拆分更合理

5. **功能改进**：
   - 更早引入用户反馈循环
   - 更完善的错误处理和恢复机制
   - 支持插件化扩展

---

## 十、应急问题处理

### Q24：如果现场演示时出现程序崩溃怎么办？

**答**：制定了完整的应急预案：

1. **事前准备**：
   - 录制完整的演示视频作为备份
   - 准备多个测试场景（不同参数配置）
   - 提前在不同环境测试

2. **事中处理**：
   - 保持冷静，说明问题可能原因
   - 尝试刷新页面/重启浏览器
   - 切换到备用演示视频
   - 解释这是虚拟仿真系统的特点，实际使用中需要稳定的计算环境

3. **事后说明**：
   - 坦诚说明问题原因
   - 介绍容错机制和改进措施
   - 强调系统在实际应用中的稳定性已得到验证

---

### Q25：如何证明这个系统的实验结果是可信的？

**答**：我们从多个角度验证了系统的可信度：

1. **理论验证**：
   - 计算公式基于成熟的物理理论
   - 结果与教科书、文献中的标准值吻合
   - 声速计算精度满足工程要求

2. **实验验证**：
   - 与真实实验室的超声光栅实验结果对比
   - 相对误差控制在2%以内
   - 重复性实验结果一致

3. **交叉验证**：
   - MATLAB计算结果与JavaScript实现一致
   - 不同拟合方法的结果相互印证
   - 统计量（R²、标准差）在合理范围

4. **用户反馈**：
   - 邀请学生试用并收集反馈
   - 根据反馈优化参数和算法
   - 多次迭代验证准确性

---

## 附录：常用技术术语解释

| 术语 | 解释 |
|------|------|
| OBJ格式 | Wavefront开发的3D模型交换格式 |
| WebGL | 基于OpenGL ES的Web图形渲染技术 |
| Canvas 2D | HTML5提供的2D图形绘制API |
| RESTful API | 遵循REST架构风格的Web服务接口 |
| MEX文件 | MATLAB编译的可执行二进制文件 |
| sinc函数 | 正弦基数函数，sinc(x)=sin(πx)/(πx) |
| 最小二乘法 | 数据拟合的标准优化算法 |
| 光栅周期 | 相邻两缝之间的距离 |
| 声速 | 声波在介质中传播的速度 |
| R²值 | 决定系数，衡量拟合优度的统计量 |

---

**提示**：建议重点准备Q1、Q2、Q6、Q9、Q13、Q18、Q22这几个问题，这些是评委最可能提问的核心问题。回答时注意逻辑清晰，先讲结论再展开细节。
